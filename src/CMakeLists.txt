SET(APP_NAME crashapp)
SET(SRCS_LIST main.cpp)
#SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/breakpad/src)
ADD_EXECUTABLE(${APP_NAME} ${SRCS_LIST})
TARGET_LINK_LIBRARIES(${APP_NAME} breakpad_client pthread)
